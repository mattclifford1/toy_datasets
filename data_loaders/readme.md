# loaders
loaders (synthetic and real data) need to return a standardised format of {'X': X, 'y': y} for two dicts train and test

# todo
think about only returning the whole data and having the train/test split as a separate function to call?